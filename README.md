There are three calibration related components:

* hardware defects management ("redman")
* calibration framework ("calibtic")
* calibration logic ("cake")

This repository provides the calibration logic.
